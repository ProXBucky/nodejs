<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
</head>

<body>

    <div class="form">
        <% for(var i=0 ; i < dataUser.length; i++) { %>
            <form action="/update-user" method="post">
                <div class="header">
                    <h3>Update the user</h3>
                </div>
                <div class="form-items">
                    <label for="">First Name: </label>
                    <input type="text" name="firstName" value="<%= dataUser[i].firstName %>">
                </div>
                <div class="form-items">
                    <label for="">Last Name: </label>
                    <input type="text" name="lastName" value="<%= dataUser[i].lastName %>">
                </div>
                <div class="form-items">
                    <label for="">Address: </label>
                    <input type="text" name="address" value="<%= dataUser[i].address %>">
                </div>
                <div class="form-items">
                    <label for="">Email: </label>
                    <input type="email" name="email" value="<%= dataUser[i].email %>">
                </div>
                <input type="text" hidden value="<%= dataUser[i].id %>" name="id">
                <div class="form-items">
                    <button class="fcc-btn" type="submit">Update</button>
                </div>
            </form>
            <% } %>
    </div>

</body>

</html>